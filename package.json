{
  "name": "profile",
  "private": true,
  "description": "Matthew Law's interactive developer portfolio",
  "scripts": {
    "build:css": "tailwindcss -i ./assets/css/tailwind-source.css -o ./assets/css/tailwind.css --minify",
    "render:drafts": "node util/render-drafts.js",
    "build": "npm run build:css && npm run render:drafts && node util/compile_meta.js && node util/build.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.7",
    "tailwindcss": "^4.3.3"
  }
}
